<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="bootstrap.css">
    <title>Thank you!</title>
</head>
<body>
    <div class="container">
        <div class="row mt-3">
            <div class="col-6 offset-3 border rounded p-3">
                <p>#{username}, thanks for the order!</p>
                <p>Book "#{bookname}" will be delivered on #{date} to #{address}</p>
                <p class="d-grid"><a class="btn btn-success" href="/">Home</a></p>
            </div>
        </div>
    </div>
</body>
</html>